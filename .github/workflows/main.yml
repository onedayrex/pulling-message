name: CURL CLI ACTION
on:
  workflow_dispatch:
jobs:
  curl:
    runs-on: ubuntu-latest
    steps:
    - name: curl
      uses: enflo/curl-action@master
      with:
        curl: 'http://www.baidu.com'
